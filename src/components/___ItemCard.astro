<!-- ---
import type { CollectionEntry } from 'astro:content'

interface Props {
    name: string
    price: number
    suggestions: number
    image: string
}

const { name, price, suggestions, image } = Astro.props
---
<a href="#" class="group bg-white p-2 rounded-lg ring-1 ring-inset ring-gray-300">
    <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-lg bg-red xl:aspect-h-8 xl:aspect-w-7">
      <img src={image} alt="{name}" class="h-full w-full object-cover object-center group-hover:opacity-75">
    </div>
    <div class="flex align-middle">
        <h3 class="text-md text-gray-700">{name}</h3>
    </div>
    <h3 class="text-lg text-gray-700 font-medium">${price}</h3> 
    <p class="text-sm text-gray-500 mt-2">* {suggestions}</p>
</a> -->